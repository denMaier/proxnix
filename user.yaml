# user.yaml is only for native NixOS services.
# For containers, place Quadlet files and app config in containers/<vmid>/quadlets/.
runtime: native
services:
  jellyfin:
    enable: true
    hardware_acceleration: true
