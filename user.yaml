# user.yaml is only for native NixOS services.
# For containers, place Quadlet files in containers/<vmid>/dropins/.
runtime: native
services:
  jellyfin:
    enable: true
    hardware_acceleration: true
