[package]
name = "proxnix-host"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Host-side proxnix control binary"

[dependencies]
serde_json = "1"
uuid = { version = "1", default-features = false, features = ["v5"] }
