#!/usr/bin/env bash

set -euo pipefail

case "${1:-}" in
  remove|deconfigure)
    if command -v systemctl >/dev/null 2>&1 && [[ -d /run/systemd/system ]]; then
      systemctl disable --now proxnix-gc.timer || true
    fi
    ;;
esac

exit 0
