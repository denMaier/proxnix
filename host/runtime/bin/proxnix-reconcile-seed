#!/usr/bin/env bash
# Seed the last built desired system closure into one proxnix-managed CT.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${SCRIPT_DIR}/proxnix-reconcile" --seed-only "$@"
