#!/usr/bin/env bash
# proxnix-reconciler-state -- node-local reconciler SQLite journal helper.

set -euo pipefail

PROXNIX_HOST_BIN="${PROXNIX_HOST_BIN:-/usr/local/sbin/proxnix-host}"
exec "$PROXNIX_HOST_BIN" state "$@"
