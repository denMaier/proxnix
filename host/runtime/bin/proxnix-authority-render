#!/usr/bin/env bash
# Render /var/lib/proxnix/authority from the legacy relay cache.

set -euo pipefail

PROXNIX_HOST_BIN="${PROXNIX_HOST_BIN:-/usr/local/sbin/proxnix-host}"
exec "$PROXNIX_HOST_BIN" authority render "$@"
