# Host Extras

This directory contains optional examples for host-side systemd units, mounts,
timers, and udev rules that can be copied into a site repository and adapted.

The product install path does not deploy these files. Real host inventory,
domains, backup repositories, storage IDs, and local mount choices should live
in the site repo, not in this reusable proxnix repository.
